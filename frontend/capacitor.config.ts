import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oscc.detection',
  appName: 'OSCC Detection System',
  webDir: 'build'
};

export default config;
